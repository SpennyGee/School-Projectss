package homework04;

/**
 * Interface for viewer
 * below is a method that viewer can use
 */
public interface View {
	
	// a method that updates the DrawingModel
	void update(DrawingModel model);
}